# Lazygarde

t = int(input())
for z in range(t) :
    a = input()
    d = 0
    s = ""
    for i in a :
        if i.isdigit() : d += int(i)
        else : s += i
    print(''.join(sorted(s)), d, sep = "")
